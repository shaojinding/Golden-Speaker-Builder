- F1 = 51.1 when precision == recall == 51.1%
- The threshold for this F1 is 0.32, at threshold[41468]

# Setting
input_size = 80
hidden_size = 40
num_classes = 1
num_epochs = 100
batch_size = 128
learning_rate = 0.001
model_type = 'L2ArcticFCnetJoint'
# If is_train is False, then have to provide a path with cached model files.
is_train = True
# Path to dir that contains pre-trained models.
cache_dir = ''
is_rbm = True
# Path to pre-trained RBM model.
rbm_path = ''
